name: Common Jobs CI

on: push

jobs:
  common:
    name: 'Common jobs'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install modules
        run: yarn

      - name: Run ESLint
        run: yarn run lint

      - name: Run Tests
        run: yarn test
